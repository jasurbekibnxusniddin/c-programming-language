#include <stdio.h>
#include <stdbool.h>

int main() {
    int age;
    float height;
    double value;
    char latter;
    bool idf;

    age = 10;
    height = 120;
    value = 120.00;
    latter = 'a';

    printf("%d, %f, %f, %c, %d\n", age, height, value, latter, idf);
}